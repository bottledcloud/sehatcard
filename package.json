{
  "name": "sehatcard",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Portable, offline patient health summary for India — card + QR + FHIR R4 + SNOMED, encrypted at rest.",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
